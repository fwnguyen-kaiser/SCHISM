# SCHISM V2 — Phase -1 to Phase 2 (science only, no infra)
# Only Python + numpy/scipy + pandas/pyarrow needed until Phase 3 (DB).
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
pyarrow==24.0.0
requests==2.34.2
