# schism-shared installed separately: pip install -e /app/shared (Dockerfile) or pip install -e schism/shared (local)
numpy>=2.4
scipy>=1.17
statsmodels>=0.14
pandas>=3.0
# numba>=0.61  # uncomment to JIT-compile EM inner loop if needed
